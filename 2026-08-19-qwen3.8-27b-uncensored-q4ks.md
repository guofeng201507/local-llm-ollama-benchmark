# Qwen3.8 27B Uncensored Q4_K_S 本地测试

测试日期：2026-08-19  
设备：Apple M3，24 GB 统一内存  
Ollama：0.32.13  
模型：`mradermacher/Qwen3.8-27B-Uncensored-GGUF`  
量化：`Q4_K_S`

## 部署结果

模型可以在这台 24 GB Mac 上运行。Ollama 显示：

- 架构：`qwen35`
- 参数：27.3B
- 原生上下文：262144
- 量化：Q4_K_S
- 实际测试上下文：4096
- 加载后模型大小：约 15 GB
- 处理器分配：100% GPU（Metal）
- 能力标记：tools、thinking、completion

首次短请求的模型加载耗时约 48 秒。模型加载后没有发生 CPU 分层卸载，
但 24 GB 统一内存没有足够余量支持很长上下文。

仓库的 Q4_K_S 主文件约 15.83 GB。Ollama 还自动下载了约 629 MB 的
`mmproj-Q8_0` 视觉投影文件，因此实际磁盘下载量约 16.45 GB。

直接使用 Hugging Face 名称拉取时，两个文件达到 100% 后连续两次出现
`context deadline exceeded`。数据 blob 已完整下载，因此改用本地 GGUF
blob 创建模型，最终成功：

```text
FROM /path/to/Qwen3.8-27B-Uncensored.Q4_K_S.gguf

PARAMETER num_ctx 4096
PARAMETER temperature 0
```

```bash
ollama create qwen3.8-27b-uncensored:q4_k_s -f Modelfile
```

## 测试汇总

| 测试 | 结果 | 平均耗时 | 平均输出 |
|---|---:|---:|---:|
| GSM8K 普通模式，前 10 题 | 5/10 | 23.9 秒 | 79.9 tokens |
| GSM8K 思考模式，前 3 题 | 3/3 | 110.2 秒 | 409.3 tokens |
| BFCL v4 simple_python，前 20 题 | 16/20 | 22.0 秒 | 39.9 tokens |
| IFEval，前 10 题 Strict | 10/10，18/18 | 109.4 秒 | 284.4 tokens |
| IFEval，前 10 题 Loose | 10/10，18/18 | 同上 | 同上 |

## GSM8K

普通模式关闭思考，`temperature=0`、`num_ctx=4096`、输出上限 512。模型
只得到 5/10，低于此前相同题目的 Qwen3 8B（10/10）。多数题只输出
6–10 tokens，但有一题输出 512 tokens、耗时 134 秒后仍答错，说明输出
停止行为不稳定。

思考模式使用相同前 3 题和 1024-token 上限，结果提升到 3/3。不过第三题
耗时 266 秒、输出 998 tokens，几乎触及上限。思考模式提高了数学正确率，
但不适合要求快速响应的交互任务。

## BFCL 工具调用

simple_python 前 20 题得到 16/20，与此前 Qwen3 8B 和 Qwen3.5 9B 同分，
没有体现出 27B 的明显优势。

四个失败中：

- 两题在请求阶段返回 HTTP 400；
- 两题把 Python 幂运算 `**` 写成数学形式 `^`，导致参数不匹配。

Ollama 能正确识别并返回结构化工具调用，说明 GGUF 内嵌模板与当前版本
的基础工具调用协议兼容。

## IFEval

固定使用 Google Research 官方数据前 10 条，共 18 条可验证指令。
`temperature=0`、`think=false`、`num_ctx=4096`、输出上限 2048。

Strict 和 Loose 均为 10/10、18/18，没有截断。这与此前 Qwen3.5 9B 的
小样本满分相同，说明模型的严格格式与约束遵循能力很强。

代价是平均 109.4 秒/题，明显慢于 Qwen3.5 9B 的 64.5 秒。最后一题耗时
519 秒、输出 715 tokens，存在非常明显的长尾延迟。

## 综合判断

这个 Q4_K_S 版本在 24 GB M3 上属于“能完整装入并运行”，但不是“运行
轻松”。它最突出的优势是严格指令遵循；数学普通模式和工具调用没有表现出
与 27B 规模相称的优势。

建议用途：

- 复杂格式输出、严格约束写作；
- 可以接受分钟级等待的本地隐私任务；
- uncensored/abliterated 微调行为研究。

不建议作为：

- 日常低延迟聊天默认模型；
- 本地快速代码代理或高频工具调用模型；
- 仅凭“27B”期待稳定超过 Qwen3 8B/9B 的通用升级。

实际使用建议保持 4096 上下文、并发 1。若需要更长上下文，应换更低量化
或更大内存设备，而不是直接把该模型开到其标称 262K 上下文。

## 局限

所有测试都是固定小样本，用于发现明显可用性问题，不是完整排行榜。
该模型是社区 uncensored/abliterated 衍生版，结果不能代表 Qwen 官方原始
Qwen3.8-27B 模型。
