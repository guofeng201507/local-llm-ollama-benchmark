# 2026-07-26 本地小模型测试总结

## 1. 测试目的

本次测试最初用于验证 Nanbeige4.2-3B 在 Apple Silicon Mac 上通过
Ollama 本地运行的可行性，随后使用相同的小规模题目，进一步比较：

- Nanbeige4.2-3B Q6_K
- Google Gemma 4 E4B
- Qwen3.5 4B
- Qwen3.5 9B
- Qwen3 8B

关注点不只是模型能否得到正确答案，还包括：

- 普通模式与思考模式的准确率差异
- 延迟和输出长度
- 是否遵循“只输出最终答案”的指令
- 是否会重复推理、耗尽 Token 仍不提交答案
- 在本地 Ollama 环境中的真实可用性

## 2. 测试环境

| 项目 | 配置 |
|---|---|
| 设备 | MacBook Air，Apple M3，10 核 GPU |
| 内存 | 24 GB 统一内存 |
| 系统时区 | Asia/Singapore |
| 标准 Ollama | 0.32.1，`127.0.0.1:11434` |
| Nanbeige 专用 Ollama | 本地编译分支，`127.0.0.1:11435` |
| 评测工具 | EvalScope 1.9.1、自制 Ollama 原生 API 评测脚本 |
| 推理设备 | 所有测试模型均显示 100% GPU |
| 温度 | 0 |
| 测试上下文 | 4096 Token |

## 3. Nanbeige 本地部署过程

标准 Ollama 0.32.1 无法识别 Nanbeige 架构：

```text
unknown model architecture: 'nanbeige'
```

因此本地编译了 Nanbeige 提供的兼容分支：

```text
vendor/nanbeige-llama.cpp
vendor/nanbeige-ollama
```

兼容服务使用独立端口，避免与标准 Ollama 冲突：

```bash
./run-nanbeige-ollama.sh
```

最终保留的模型：

```text
nanbeige4.2:3b-q6
```

Q4 和 Q8 版本在完成对比后已经删除。

## 4. Nanbeige 量化版本观察

测试过 Q4_K_M、Q6_K 和 Q8 版本：

- Q4_K_M 文件最小、速度较快，但量化损失风险最高。
- Q6_K 在体积、质量和速度之间最均衡，最终保留。
- Q8 更接近原始权重，但文件更大，实际测试没有产生足以抵消资源开销的优势。
- 在 24 GB M3 设备上三个版本都能运行。

最终 Q6_K 的本地情况：

| 项目 | 结果 |
|---|---|
| 模型文件 | 约 3.4 GB |
| Ollama 加载大小 | 约 4.2 GB |
| 处理器 | 100% GPU |
| 实测生成速度 | 约 9–15 Token/s |

早期功能测试发现：

- 水管长度算术题能够答对，但答案明显过度展开。
- 普通模式比较 `9.11` 和 `9.8` 时出现基础错误。
- 开启思考后，Q4、Q6、Q8 都能答对该小数比较题。
- 思考模式需要约 54–75 秒，并生成约 570–804 Token。

这些结果表明思考模式能够修正部分错误，但代价过高。

## 5. 统一 GSM8K 测试方法

使用 GSM8K 官方测试集开头的固定样本，所有模型使用相同题目。

普通模式：

- 前 10 题
- `think=false`
- 温度 0
- 上下文 4096
- Nanbeige 初始测试上限 128 Token
- 后续公平对比主要使用 512 Token

思考模式：

- 相同的前 3 题
- `think=true`
- 温度 0
- 上下文 4096
- 输出上限 1024 Token

统一提示：

```text
{question}
请计算并只输出最终答案，格式必须是：\boxed{数字}
```

严格判分规则：

- 必须在最终回答中实际提交正确数字。
- 隐藏思考中算出正确答案、但没有输出最终答案，仍然判错。
- 被 Token 上限截断的答案判错。

这一规则更接近实际 API 使用：调用方只能依赖最终响应，不能把未完成的
内部推理视为成功。

## 6. 普通模式结果

| 模型 | 参数与量化 | 正确率 | 平均耗时 | 平均输出 |
|---|---|---:|---:|---:|
| Qwen3 8B | 8.2B，Q4_K_M | **10/10** | 16.43 秒 | 188 Token |
| Gemma 4 E4B | 8.0B 总参数，Q4_K_M | 6/10 | 22.62 秒 | 353 Token |
| Qwen3.5 4B | 4.7B，Q4_K_M | 4/10 | 4.25 秒 | 44 Token |
| Qwen3.5 9B | 9.7B，Q4_K_M | 2/10 | **2.03 秒** | **4.7 Token** |
| Nanbeige Q6 | 4.17B，Q6_K | 2/10 | 2.87 秒 | 32 Token |

### 普通模式解读

#### Qwen3 8B

前 10 题全部答对，是本次唯一取得 10/10 的模型。它没有依赖隐藏思考，
而是在正常回答中给出相对简洁的计算过程。准确率、速度和输出长度之间的
平衡最好。

#### Gemma 4 E4B

在 512 Token 上限下得到 6/10，数学能力明显强于 Nanbeige 和 Qwen3.5
小模型，但经常无视“只输出答案”，导致平均输出较长。

Gemma 曾在 128 Token 上限下得到 0/10，因为 10 道题全部在最终答案之前
被截断。这个 0/10 体现了指令遵循和简洁性问题，但不作为数学能力的正式
对比结果，因此表中使用 512 Token 的 6/10。

#### Qwen3.5 4B

得到 4/10。速度快、输出简洁，但数学可靠性不足。它在资源规模上是真正
接近 4B 的模型，而 Gemma E4B 实际包含约 8B 总参数。

#### Qwen3.5 9B

普通模式严格遵守“只输出答案”，平均仅输出 4.7 Token，但多数题直接给出
错误数字。增加参数量没有在这组题上转化为普通模式准确率。

#### Nanbeige Q6

仅得到 2/10。结合早期的小数比较错误，说明关闭思考时基础推理可靠性不足。

## 7. 思考模式结果

| 模型 | 正确率 | 平均耗时 | 平均输出 | 主要问题 |
|---|---:|---:|---:|---|
| Gemma 4 E4B | **3/3** | **59.00 秒** | **515 Token** | 输出仍偏长，但能停止并提交答案 |
| Nanbeige Q6 | 2/3 | 170.33 秒 | 585 Token | 第三题算对但未提交答案 |
| Qwen3 8B | 1/3 | 64.08 秒 | 803 Token | 重复检查正确计算，耗尽上限 |
| Qwen3.5 4B | 1/3 | 79.46 秒 | 807 Token | 两题耗尽 1024 Token |
| Qwen3.5 9B | 1/3 | 70.23 秒 | 806 Token | 两题算对但未提交答案 |

### 思考模式解读

Gemma 4 E4B 是唯一在相同 1024 Token 预算内完成 3/3 的模型。它仍然比较
啰嗦，但能够可靠结束推理并提交最终答案。

Qwen3、Qwen3.5 4B 和 Qwen3.5 9B 都出现类似退化：

1. 很早已经算出正确答案。
2. 不断重新检查题意、格式和可能的歧义。
3. 反复确认同一计算。
4. 耗尽 1024 Token。
5. 没有输出最终答案，严格判错。

Qwen3.5 9B 额外进行了 2048 Token 诊断：

| 指标 | 结果 |
|---|---:|
| 正确率 | 1/3 |
| 平均耗时 | 203.48 秒 |
| 平均输出 | 1488 Token |

扩大预算没有解决问题。第一题和第三题仍然耗尽 2048 Token，说明这不是
“只差一点输出空间”，而是当前 Ollama 模板或模型停止行为发生退化。

Qwen3 8B 的情况尤其值得注意：普通模式 10/10，显式思考却只有 1/3。
因此该模型应优先关闭思考使用。

## 8. EvalScope 冒烟测试

EvalScope 使用 OpenAI 兼容接口对 Nanbeige 运行了 3 道 GSM8K 冒烟测试：

| 指标 | 结果 |
|---|---:|
| 准确率 | 2/3 |
| 平均延迟 | 170.71 秒 |
| 平均输入 | 717 Token |
| 平均输出 | 897 Token |
| 平均吞吐 | 5.25 Token/s |

第三题在隐藏推理中算出正确答案，但达到 1024 Token 上限时最终回答为空，
因此判错。该测试也证明了答案提取和严格评分流程工作正常。

## 9. 模型规模与本地占用

| 模型 | Ollama 参数统计 | 量化 | 下载大小 | 实测加载大小 |
|---|---:|---|---:|---:|
| Nanbeige4.2 Q6 | 4.17B | Q6_K | 3.4 GB | 4.2 GB |
| Qwen3.5 4B | 4.7B | Q4_K_M | 3.4 GB | 3.2 GB |
| Qwen3 8B | 8.2B | Q4_K_M | 5.2 GB | 5.9 GB |
| Qwen3.5 9B | 9.7B | Q4_K_M | 6.6 GB | 5.6 GB |
| Gemma 4 E4B | 8.0B 总参数 | Q4_K_M | 9.6 GB | 3.3 GB |

Gemma 的 E4B 名称表示约 4.5B 核心或有效参数，但包含嵌入后总参数约 8B。
因此：

- 按核心计算量，它可以与 4B 级模型比较。
- 按总容量，它更接近 Qwen3 8B 或 Qwen3.5 9B。

## 10. 最终结论

### 综合排名

按本次本地实际可用性：

1. **Qwen3 8B，关闭思考**
2. **Gemma 4 E4B**
3. **Qwen3.5 4B**
4. **Qwen3.5 9B**
5. **Nanbeige4.2 Q6**

### 推荐使用方式

#### Qwen3 8B

本次测试的最佳日常选择：

```bash
ollama run qwen3:8b --think=false
```

适合普通数学、逻辑、问答和本地通用任务。显式思考模式在当前环境下反而
降低可靠性，不建议默认开启。

#### Gemma 4 E4B

适合确实需要显式长推理的任务。它在相同思考预算下最稳定，但普通回答偏长，
而且实际总参数和下载体积并不是真正的 4B 级。

#### Qwen3.5 4B

适合对速度和内存更敏感、错误可以被外部验证的简单任务。不建议将其作为
可靠数学推理模型。

#### Qwen3.5 9B

当前 Ollama 0.32.1 环境下没有体现出相对 Qwen3 8B 的优势。普通模式过于
直接，思考模式又无法及时停止。若继续研究，应优先检查新版 Ollama、官方
推荐聊天模板或其他推理框架，而不是继续增加 Token 上限。

#### Nanbeige4.2 Q6

不建议作为日常通用助手、代码模型或可靠推理模型。它可以用于架构研究、
兼容性验证和小模型实验，但当前实用性明显不足。

## 11. 关于“10B 以下是否值得使用”

本次结果不支持“10B 以下全部没有测试必要”的绝对结论：

- Nanbeige 约 4B 表现不足。
- Qwen3.5 4B 有一定速度价值，但准确率有限。
- Qwen3 8B 在相同 10 题上达到 10/10，并且优于更晚的 Qwen3.5 9B。

决定可用性的因素不仅是参数量，还包括训练质量、后训练方式、聊天模板、
思考控制和停止行为。小模型仍适合分类、抽取、改写、路由、结构化输出和
本地隐私处理；但复杂推理必须经过实际任务验证。

## 12. 测试局限

这是一轮用于选型的本地小样本测试，不是正式模型排行榜：

- 普通模式只有 GSM8K 前 10 题。
- 思考模式只有相同前 3 题。
- 没有进行随机抽样或多次重复。
- 没有覆盖中文知识、代码、工具调用、视觉和长上下文。
- 不同模型的官方聊天模板可能影响思考和停止行为。
- 本次结论只直接适用于当前量化、Ollama 0.32.1、M3 24 GB和测试参数。

小样本不足以估计完整 GSM8K 分数，但足以发现明显的可用性问题，例如：

- 基础题答错
- 不遵守输出格式
- 重复思考
- 延迟过高
- 耗尽 Token 仍不提交答案

## 13. 后续测试集与评测路线图

今天的 GSM8K 小样本主要验证了基础数学、延迟和思考停止行为。后续如果继续
评测，建议按下面的顺序扩展，而不是只增加 GSM8K 题量。

### 第一优先级：实际可用性

| 测试集 | 测试内容 | 建议首轮规模 | 记录指标 |
|---|---|---:|---|
| [IFEval](https://github.com/google-research/google-research/tree/master/instruction_following_eval) | 字数、格式、关键词和禁止项等严格指令遵循 | 100 题，再扩展至全量 541 题 | 严格通过率、格式错误率、超长率 |
| [EvalPlus](https://github.com/evalplus/evalplus) | HumanEval+、MBPP+ 代码生成 | HumanEval+ 先跑 20 题 | Pass@1、编译率、执行超时率、延迟 |
| [BFCL](https://github.com/ShishirPatil/gorilla/tree/main/berkeley-function-call-leaderboard) | 函数选择、参数生成、并行和多轮工具调用 | 单轮简单/多函数共 100 题 | AST 准确率、可执行率、虚假调用率 |

IFEval 应优先进行，因为今天所有模型都不同程度出现了不遵守“只输出最终
答案”、输出过长或无法停止的问题。

EvalPlus 会执行模型生成的代码，必须使用 Docker 或其他隔离沙箱，不能直接
在主机环境运行未知代码。

### 第二优先级：知识与推理

| 测试集 | 测试内容 | 建议首轮规模 | 注意事项 |
|---|---|---:|---|
| [GSM8K](https://github.com/openai/grade-school-math) | 多步小学数学 | 固定随机种子抽样 100–300 题 | 同时记录普通/思考模式和输出截断 |
| [C-Eval](https://github.com/hkust-nlp/ceval) | 中文考试与专业知识 | 100–300 题 | 按学科分组，避免只报告总平均 |
| [CMMLU](https://github.com/haonan-li/CMMLU) | 中文通识与专业知识 | 100–300 题 | 与 C-Eval 使用相同采样和答案提取 |
| [ARC](https://github.com/allenai/ai2_arc) | 英文科学推理和选择题 | 100–300 题 | 可直接通过 EvalScope 运行 |

GSM8K 下一轮应使用固定随机种子抽样，而不是继续只取数据集开头，以降低
样本顺序偏差。建议普通模式先跑 100 题；只对普通模式失败题开启思考复测，
用于计算“思考带来的净正确率增益”和额外时间成本。

### 第三优先级：长上下文

| 测试集 | 测试内容 | 建议首轮规模 | 前置条件 |
|---|---|---:|---|
| [LongBench](https://github.com/THUDM/LongBench) | 长文档问答、摘要、检索和代码 | 先选 20 个短/中长度样本 | 上下文至少 8K，建议 16K |
| LongBench v2 | 8K 至超长上下文综合推理 | 暂不全量 | 完整测试耗时和内存成本很高 |

今天的统一测试上下文只有 4096 Token，因此不能用来评价官方宣称的 128K、
256K 等长上下文能力。长上下文测试必须单独记录：

- 实际输入 Token 数
- 首 Token 延迟
- 峰值内存
- 上下文长度增加后的生成速度
- 文档中间信息的召回率

### 推荐评测框架

#### [EvalScope](https://github.com/modelscope/evalscope)

ModelScope 官方评测框架，今天已安装 1.9.1，并成功连接 Ollama 的 OpenAI
兼容接口运行 GSM8K。适合统一管理 GSM8K、ARC、中文知识等标准测试。

需要注意：今天发现 OpenAI 兼容接口可能默认返回思考过程，且无法像 Ollama
原生 `/api/chat` 那样稳定控制 `think=false`。普通/思考模式配对测试应继续
优先使用原生 API 或增加一层明确传递思考开关的适配器。

#### [lm-evaluation-harness](https://github.com/EleutherAI/lm-evaluation-harness)

覆盖大量学术基准，适合正式、可复现的标准分数。但需要仔细确认聊天模板、
答案提取和 API 参数，避免模型能力与评测配置混在一起。

### 建议的下一轮最小测试矩阵

| 维度 | 测试集 | 样本量 |
|---|---|---:|
| 指令遵循 | IFEval | 100 |
| 数学推理 | GSM8K 随机样本 | 100 |
| 中文知识 | C-Eval 或 CMMLU | 100 |
| 代码 | HumanEval+ | 20 |
| 工具调用 | BFCL 单轮子集 | 30–100 |
| 长上下文 | LongBench 短/中样本 | 20 |

所有测试建议固定：

- 模型版本和量化方式
- Ollama 版本
- 聊天模板
- 温度 0
- 随机种子
- 上下文长度
- 最大输出 Token
- 普通/思考模式开关

除正确率外，应统一记录：

- P50/P95 延迟
- 输入、思考和最终输出 Token 数
- 生成吞吐
- 峰值内存和 GPU 占用
- 格式错误率
- 超长或截断率
- 多次运行结果波动

## 14. 文件与复现

核心评测脚本：

```text
eval_gsm8k_ollama.py
eval_nanbeige_smoke.py
```

Nanbeige 服务启动脚本：

```text
run-nanbeige-ollama.sh
```

详细逐题记录：

```text
eval-results/custom/
```

EvalScope 报告：

```text
eval-results/reports/report.html
```

Python 评测环境：

```text
.venv-eval/
```
